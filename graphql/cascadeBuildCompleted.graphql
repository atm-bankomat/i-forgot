subscription BuildCompleted {
    Build {
        name
        status
        commit {
            sha
        }
        push {
            branch
        }
        repo {
            name
            owner
        }
    }
}
