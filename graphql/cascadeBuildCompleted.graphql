subscription BuildCompleted {
    Build {
        name
        buildUrl
        status
        commit {
            sha
        }
        push {
            branch
        }
        repo {
            name
            owner
        }
    }
}
