query PrWithBranch($repoName: String!, $branch: String!) {
    PullRequest(state: "open") {
        repo(name: $repoName) {
            name
        }
        branch(name: $branch) {
            name
        }
    }
}
